import { beforeEach,describe,expect,it } from 'vitest';import { createProject } from '../src/project/defaults/createProject';import { useEditorStore } from '../src/project/store/editorStore';
describe('editor history',()=>{beforeEach(()=>useEditorStore.setState({project:createProject(),selectedLayerId:null,past:[],future:[],transactionStart:null}));it('adds, duplicates and removes layers',()=>{useEditorStore.getState().addTextLayer();expect(useEditorStore.getState().project.layers).toHaveLength(1);useEditorStore.getState().duplicateSelected();expect(useEditorStore.getState().project.layers).toHaveLength(2);useEditorStore.getState().deleteSelected();expect(useEditorStore.getState().project.layers).toHaveLength(1)});it('undoes and redoes changes',()=>{useEditorStore.getState().addTextLayer();useEditorStore.getState().undo();expect(useEditorStore.getState().project.layers).toHaveLength(0);useEditorStore.getState().redo();expect(useEditorStore.getState().project.layers).toHaveLength(1)});it('records a live transform as one history entry',()=>{useEditorStore.getState().addTextLayer();const id=useEditorStore.getState().selectedLayerId!,before=useEditorStore.getState().past.length;useEditorStore.getState().beginTransaction();const layer=useEditorStore.getState().project.layers[0];useEditorStore.getState().updateTransformLive(id,{...layer.transform,x:900});useEditorStore.getState().updateTransformLive(id,{...layer.transform,x:1000});useEditorStore.getState().commitTransaction();expect(useEditorStore.getState().past.length).toBe(before+1);useEditorStore.getState().undo();expect(useEditorStore.getState().project.layers[0].transform.x).toBe(610)})});
